/** @format */

import {AppRegistry} from 'react-native';
import BianminApp from './BianminApp';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => BianminApp);
